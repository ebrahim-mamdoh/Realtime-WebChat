# دليل إعداد Supabase - تطبيق الدردشة

## 📋 الخطوات المطلوبة

### 1. إنشاء حساب Supabase

1. اذهب إلى [https://supabase.com](https://supabase.com)
2. اضغط على "Start your project"
3. سجّل حساب جديد باستخدام GitHub أو البريد الإلكتروني

### 2. إنشاء مشروع جديد

1. بعد تسجيل الدخول، اضغط على "New Project"
2. اختر اسم للمشروع (مثلاً: `realtime-chat`)
3. اختر كلمة مرور قوية لقاعدة البيانات (احتفظ بها في مكان آمن)
4. اختر المنطقة الأقرب لك (Region)
5. اضغط على "Create new project"
6. انتظر 1-2 دقيقة حتى يتم إنشاء المشروع

### 3. الحصول على مفاتيح المشروع

1. من لوحة التحكم، اذهب إلى **Settings** > **API**
2. ستجد:
   - **Project URL**: انسخه
   - **anon/public key**: انسخه

### 4. إضافة المفاتيح للمشروع

1. افتح ملف `.env.local` في جذر المشروع
2. عدّل القيم كالتالي:

```env
NEXT_PUBLIC_SUPABASE_URL=YOUR_PROJECT_URL_HERE
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE
```

3. استبدل `YOUR_PROJECT_URL_HERE` و `YOUR_ANON_KEY_HERE` بالقيم التي نسختها

### 5. تشغيل SQL لإنشاء قاعدة البيانات

1. من لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. اضغط على "New query"
3. افتح ملف `supabase-schema.sql` من مشروعك
4. انسخ كل المحتوى والصقه في SQL Editor
5. اضغط على "Run" أو اضغط `Ctrl+Enter`
6. تأكد من ظهور رسالة "Success"

### 6. تفعيل Realtime للجداول

1. اذهب إلى **Database** > **Replication**
2. ابحث عن الجداول التالية وفعّل Realtime لها:
   - `users`
   - `conversations`
   - `messages`
   - `typing_indicators`
3. لكل جدول، اضغط على الزر بجانبه لتفعيل Realtime

### 7. تثبيت المكتبات المطلوبة

افتح Terminal واكتب:

```bash
npm install
```

سيقوم بتثبيت جميع المكتبات بما فيها `@supabase/supabase-js`

### 8. تشغيل التطبيق

```bash
npm run dev
```

افتح المتصفح على: [http://localhost:3000](http://localhost:3000)

## ✅ التحقق من الإعداد

### اختبار التسجيل:

1. افتح التطبيق في المتصفح
2. اضغط على "Sign up"
3. أدخل البريد الإلكتروني وكلمة المرور
4. اضغط على "Sign up"

### التحقق من قاعدة البيانات:

1. اذهب إلى **Table Editor** في Supabase
2. افتح جدول `users`
3. يجب أن تجد المستخدم الجديد

## 🔧 حل المشاكل الشائعة

### خطأ: "Invalid API key"

- تأكد من نسخ المفاتيح بشكل صحيح
- تأكد من عدم وجود مسافات في بداية أو نهاية المفاتيح
- أعد تشغيل الخادم بعد تعديل `.env.local`

### خطأ: "Table does not exist"

- تأكد من تشغيل SQL بنجاح
- اذهب إلى Table Editor وتحقق من وجود الجداول

### خطأ في Realtime

- تأكد من تفعيل Realtime لكل الجداول
- انتظر دقيقة واحدة بعد التفعيل

### خطأ في رفع الصور

- تأكد من تشغيل SQL الخاص بـ Storage
- تحقق من وجود bucket باسم `chat-images` في **Storage**

## 📊 بنية قاعدة البيانات

### جدول Users (المستخدمين)
- `id`: معرّف المستخدم
- `display_name`: اسم العرض
- `email`: البريد الإلكتروني
- `avatar_url`: رابط الصورة الشخصية
- `last_seen`: آخر ظهور
- `is_anonymous`: هل المستخدم مجهول
- `created_at`: تاريخ الإنشاء

### جدول Conversations (المحادثات)
- `id`: معرّف المحادثة
- `participants`: قائمة المشاركين
- `last_message`: آخر رسالة
- `last_message_time`: وقت آخر رسالة
- `updated_at`: تاريخ التحديث

### جدول Messages (الرسائل)
- `id`: معرّف الرسالة
- `conversation_id`: معرّف المحادثة
- `sender_id`: معرّف المرسل
- `text`: نص الرسالة
- `image_url`: رابط الصورة (اختياري)
- `created_at`: تاريخ الإنشاء

### جدول Typing Indicators (مؤشرات الكتابة)
- `conversation_id`: معرّف المحادثة
- `user_id`: معرّف المستخدم
- `updated_at`: تاريخ التحديث

## 🔐 الأمان

- جميع الجداول محمية بـ Row Level Security (RLS)
- المستخدمون يمكنهم رؤية البيانات المتعلقة بهم فقط
- الصور محمية بسياسات Storage

## 💰 الخطة المجانية

Supabase توفر خطة مجانية تشمل:
- 500 MB مساحة قاعدة بيانات
- 1 GB مساحة تخزين للملفات
- 2 GB نقل بيانات شهريًا
- Realtime غير محدود
- 50,000 مستخدم نشط شهريًا

**كافية لبناء وتجربة التطبيق!**

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع [Supabase Documentation](https://supabase.com/docs)
2. تحقق من [Supabase Community](https://github.com/supabase/supabase/discussions)
