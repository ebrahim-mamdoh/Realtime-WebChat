# كيفية استخدام التطبيق - دليل سريع

## 🎯 الخطوات البسيطة للبدء

### 1️⃣ **تثبيت المكتبات**
```bash
npm install
```

### 2️⃣ **إعداد Supabase**

#### أ. إنشاء مشروع:
1. اذهب إلى [supabase.com](https://supabase.com)
2. سجّل حساب جديد (مجاني)
3. اضغط "New Project"
4. اختر اسم + كلمة مرور + منطقة
5. انتظر 1-2 دقيقة

#### ب. نسخ المفاتيح:
1. اذهب إلى **Settings** > **API**
2. انسخ:
   - `Project URL`
   - `anon public key`

#### ج. إضافة المفاتيح:
افتح `.env.local` وضع:
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### د. إنشاء قاعدة البيانات:
1. اذهب إلى **SQL Editor** في Supabase
2. انسخ كل محتوى ملف `supabase-schema.sql`
3. الصقه في SQL Editor
4. اضغط **Run** (أو Ctrl+Enter)
5. انتظر حتى ترى "Success"

#### هـ. تفعيل Realtime:
1. اذهب إلى **Database** > **Replication**
2. فعّل Realtime لكل جدول:
   - ☑️ `users`
   - ☑️ `conversations`
   - ☑️ `messages`
   - ☑️ `typing_indicators`

### 3️⃣ **تشغيل التطبيق**
```bash
npm run dev
```

افتح: [http://localhost:3000](http://localhost:3000)

## 🎨 استخدام التطبيق

### التسجيل:
1. افتح الصفحة الرئيسية
2. اضغط "Sign up"
3. أدخل:
   - البريد الإلكتروني
   - كلمة المرور
   - اسم العرض
4. اضغط "Sign up"

أو:
- اضغط "Continue as Guest" للدخول كضيف (بدون تسجيل)

### بدء محادثة:
1. بعد تسجيل الدخول، سترى قائمة المستخدمين
2. اضغط على أي مستخدم
3. اكتب رسالتك في الأسفل
4. اضغط Enter أو زر الإرسال

### إرسال صورة:
1. افتح محادثة
2. اضغط على زر الصورة 📷
3. اختر صورة من جهازك
4. انتظر الرفع ثم الإرسال

### مؤشر الكتابة:
- عندما تبدأ الكتابة، يرى الطرف الآخر "is typing..."
- يظهر في الوقت الفعلي

### حالة الاتصال:
- **🟢 Online**: المستخدم متصل الآن
- **🔴 Offline**: آخر ظهور منذ وقت

## 🧪 اختبار التطبيق

### اختبار بمستخدمين مختلفين:

1. افتح متصفح عادي:
   - سجّل دخول كمستخدم 1
   
2. افتح متصفح في وضع خاص (Incognito):
   - سجّل دخول كمستخدم 2

3. ابدأ محادثة بينهما:
   - اكتب رسالة من المستخدم 1
   - ستظهر فورًا عند المستخدم 2
   - العكس صحيح

4. جرّب:
   - ✅ إرسال رسائل
   - ✅ رفع صور
   - ✅ مؤشر الكتابة
   - ✅ حالة Online/Offline

## 📱 الميزات المتاحة

### ✅ تم تنفيذها:
- [x] تسجيل دخول وخروج
- [x] التسجيل كمستخدم جديد
- [x] الدخول كضيف (مجهول)
- [x] قائمة المستخدمين
- [x] بدء محادثات جديدة
- [x] إرسال رسائل نصية
- [x] إرسال صور
- [x] الرسائل الفورية (Real-time)
- [x] مؤشرات الكتابة
- [x] حالة الاتصال (Online/Offline)
- [x] آخر ظهور
- [x] قائمة المحادثات
- [x] واجهة تشبه WhatsApp

## 🎨 البنية التقنية

### Frontend:
- **Next.js 14** (Pages Router)
- **React 18**
- **TypeScript**
- **Tailwind CSS**

### Backend:
- **Supabase** (PostgreSQL)
- **Supabase Auth**
- **Supabase Storage**
- **Supabase Realtime**

### الميزات:
- Row Level Security (RLS)
- Real-time subscriptions
- Image storage
- User presence system

## 🔐 الأمان

- ✅ كل مستخدم يرى بياناته فقط
- ✅ المحادثات محمية
- ✅ الصور في Storage محمي
- ✅ RLS مفعّل على كل الجداول

## 💡 نصائح

1. **للتطوير**:
   - استخدم `.env.local` للمتغيرات
   - لا ترفع `.env.local` إلى Git

2. **للإنتاج**:
   - ضع المتغيرات في Vercel/Netlify
   - فعّل HTTPS

3. **الأداء**:
   - Supabase مجاني حتى 500 MB
   - كافي لآلاف الرسائل

## 🐛 حل المشاكل

### المشكلة: "Invalid API key"
**الحل**: تأكد من نسخ المفاتيح بشكل صحيح من Supabase

### المشكلة: "Table does not exist"
**الحل**: شغّل SQL من ملف `supabase-schema.sql`

### المشكلة: الرسائل لا تظهر فورياً
**الحل**: فعّل Realtime للجداول من Database > Replication

### المشكلة: لا يمكن رفع الصور
**الحل**: تحقق من وجود bucket باسم `chat-images` في Storage

### المشكلة: خطأ في تسجيل الدخول
**الحل**: تحقق من:
1. Supabase URL صحيح
2. Anon Key صحيح
3. Email Confirmations معطّل (في Auth Settings)

## 📞 الدعم

إذا واجهت مشكلة:
1. راجع ملف `SUPABASE_SETUP.md`
2. راجع ملف `MIGRATION_COMPLETE.md`
3. تحقق من [Supabase Docs](https://supabase.com/docs)

## 🎉 جاهز!

الآن لديك تطبيق دردشة فوري كامل الميزات، مجاني تماماً! 🚀
