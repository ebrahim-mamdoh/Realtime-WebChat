# ⚠️ يجب إعداد Supabase أولاً!

## الخطأ الحالي: "Invalid supabaseUrl"

التطبيق يحتاج إلى مفاتيح Supabase للعمل.

---

## ⚡ الحل السريع (5 دقائق):

### 1️⃣ إنشاء حساب Supabase مجاني

👉 اذهب إلى: **https://supabase.com**

1. اضغط **"Start your project"**
2. سجّل حساب جديد (بالبريد أو GitHub)

---

### 2️⃣ إنشاء مشروع جديد

1. اضغط **"New Project"**
2. املأ البيانات:
   - **Name**: `realtime-chat` (أو أي اسم تريده)
   - **Database Password**: اختر كلمة مرور قوية (احتفظ بها!)
   - **Region**: اختر الأقرب لك
3. اضغط **"Create new project"**
4. **انتظر 1-2 دقيقة** حتى يتم إنشاء المشروع

---

### 3️⃣ الحصول على المفاتيح

1. من قائمة المشروع، اذهب إلى:
   ```
   Settings (الإعدادات) → API
   ```

2. ستجد:
   - **Project URL** - انسخه
   - **anon public** key - انسخه

مثال:
```
Project URL: https://abcdefghijklm.supabase.co
anon key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFz...
```

---

### 4️⃣ إضافة المفاتيح للتطبيق

افتح ملف `.env.local` في جذر المشروع وعدّل:

```env
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3M...
```

**⚠️ مهم**: استبدل القيم بالمفاتيح الحقيقية من مشروعك!

---

### 5️⃣ إنشاء قاعدة البيانات

1. في Supabase Dashboard، اذهب إلى:
   ```
   SQL Editor (محرر SQL)
   ```

2. اضغط **"New query"**

3. افتح ملف `supabase-schema.sql` من المشروع

4. **انسخ كل محتوى الملف** والصقه في SQL Editor

5. اضغط **"Run"** أو `Ctrl+Enter`

6. تأكد من ظهور: ✅ **"Success. No rows returned"**

---

### 6️⃣ تفعيل Anonymous Sign-in (تسجيل الضيف)

1. اذهب إلى:
   ```
   Authentication → Providers
   ```

2. ابحث عن **"Anonymous Sign-ins"**

3. فعّل الخيار: **Enable anonymous sign-ins**

4. **لإلغاء التأخير 36 ثانية (اختياري):**
   - في نفس الصفحة، اذهب لـ **"Rate Limits"**
   - ابحث عن **"Anonymous sign-ins per hour"**
   - غيّر من `30` إلى `999`
   - اضغط **Save**

5. اضغط **Save**

---

### 7️⃣ تفعيل Realtime

1. اذهب إلى:
   ```
   Database → Replication
   ```

2. ابحث عن الجداول التالية وفعّل Realtime لها:
   - ☑️ **users**
   - ☑️ **conversations**
   - ☑️ **messages**
   - ☑️ **typing_indicators**

3. لكل جدول، اضغط الزر بجانبه لتفعيل Realtime

---

### 8️⃣ إعادة تشغيل التطبيق

في Terminal:

```bash
# أوقف الخادم (Ctrl+C)
# ثم شغّله مرة أخرى:
npm run dev
```

افتح: **http://localhost:3000**

---

## ✅ اختبر التطبيق

1. افتح التطبيق
2. اضغط **"Sign up"**
3. أدخل بريد إلكتروني وكلمة مرور
4. يجب أن يعمل بدون أخطاء! 🎉

---

## 🐛 المشاكل الشائعة

### ❌ لا يزال هناك خطأ "Invalid URL"
**الحل**: 
- تأكد من نسخ URL بالكامل (يبدأ بـ https://)
- تأكد من عدم وجود مسافات في البداية أو النهاية
- أعد تشغيل الخادم: `npm run dev`

### ❌ "Table does not exist"
**الحل**: تأكد من تشغيل SQL Schema في الخطوة 5

### ❌ الرسائل لا تظهر فورياً
**الحل**: فعّل Realtime في الخطوة 7

### ❌ "new row violates row-level security policy"
**الحل السريع**: 
شغّل هذا SQL في **SQL Editor**:
```sql
drop policy if exists "Users can insert own data" on public.users;
create policy "Users can insert own data"
  on public.users for insert
  with check (auth.uid() = id);
```

**إذا استمرت المشكلة**، استخدم هذا بدلاً منه:
```sql
drop policy if exists "Users can insert own data" on public.users;
create policy "Users can insert own data"
  on public.users for insert
  with check (true);
```

ثم أعد تشغيل التطبيق: `npm run dev`

---

## 📚 للمزيد من التفاصيل

اقرأ: **[SUPABASE_SETUP.md](SUPABASE_SETUP.md)** - دليل كامل مفصّل

---

## 💡 نصيحة

Supabase **مجاني 100%** ولا يطلب بطاقة ائتمان!

الخطة المجانية تشمل:
- ✅ 500 MB قاعدة بيانات
- ✅ 1 GB تخزين ملفات
- ✅ Realtime غير محدود
- ✅ 50,000 مستخدم نشط/شهر

**كافي تماماً لتطبيقك!** 🚀
